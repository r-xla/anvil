#' @param backend (`character(1)`)\cr
#'   Backend used to construct the array. `"auto"` (default) picks a backend
#'   based on the inputs (or [`default_backend()`] when there are none).
#'   See [`jit()`] for the list of available backends.
