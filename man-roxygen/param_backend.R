#' @param backend (`character(1)`)\cr
#'   Backend used to construct the array. `"auto"` picks a backend
#'   based on the inputs (or [`default_backend()`] when there are none).
#'   Other backends are `"xla"` and `"quickr"` (experimental).
#'   
