# p_eq, p_ne, p_gt, p_ge, p_lt, p_le

    x gradient can only be computed for functions that return float scalar
    i Got dtype=i1

---

    x gradient can only be computed for functions that return float scalar
    i Got dtype=i1

---

    x gradient can only be computed for functions that return float scalar
    i Got dtype=i1

---

    x gradient can only be computed for functions that return float scalar
    i Got dtype=i1

---

    x gradient can only be computed for functions that return float scalar
    i Got dtype=i1

---

    x gradient can only be computed for functions that return float scalar
    i Got dtype=i1

