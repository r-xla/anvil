# Unflatten

Reconstruct a nested structure from a flat list using a tree structure.

## Usage

``` r
unflatten(node, x)
```

## Arguments

- node:

  Tree node describing the structure.

- x:

  Flat list to unflatten.

## Value

Reconstructed nested structure.
