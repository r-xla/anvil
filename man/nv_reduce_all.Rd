% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_reduce_all}
\alias{nv_reduce_all}
\title{All Reduction}
\usage{
nv_reduce_all(operand, dims, drop = TRUE)
}
\arguments{
\item{operand}{(\code{\link{tensorish}})\cr
Operand.}

\item{dims}{(\code{integer()})\cr
Dimensions to reduce.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop reduced dimensions.}
}
\value{
\code{\link{tensorish}}\cr
Boolean tensor.
When \code{drop = TRUE}, the reduced dimensions are removed.
When \code{drop = FALSE}, the reduced dimensions are set to 1.
}
\description{
Performs logical AND along the specified dimensions.
Returns \code{TRUE} only if all elements are \code{TRUE}.
}
\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  x <- nv_tensor(matrix(c(TRUE, FALSE, TRUE, TRUE), nrow = 2))
  nv_reduce_all(x, dims = 1L)
})
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nvl_reduce_all]{nvl_reduce_all()}} for the underlying primitive.
}
