% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_lu}
\alias{nv_lu}
\title{LU Decomposition}
\usage{
nv_lu(a)
}
\arguments{
\item{a}{(\code{\link{arrayish}})\cr
Matrix of data type floating-point with exactly 2 dimensions.}
}
\value{
Named \code{list} with elements \code{LU} (shape \verb{(m, n)}, same dtype as
\code{a}) and \code{pivots} (length \code{k}, dtype \code{i32}).
}
\description{
Computes the partial-pivoted LU decomposition of a matrix.
Factors a matrix \code{a} of shape \verb{(m, n)} into a packed \code{LU} of the same
shape and a vector of \code{pivots} of length \code{k = min(m, n)}, such that
\verb{P a = L U}, where \code{L} is lower-unit-triangular \verb{(m, k)}, \code{U} is upper-
triangular \verb{(k, n)}, and \code{P} is the row permutation encoded by \code{pivots}
(1-based row swaps applied during elimination).

Uses a LAPACK / cuSOLVER custom call via the XLA FFI.
}
\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  x <- nv_array(matrix(c(4, 3, 6, 3), nrow = 2), dtype = "f64")
  nv_lu(x)
})
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nvl_lu]{nvl_lu()}}
}
