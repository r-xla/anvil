% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_cummin}
\alias{prim_cummin}
\title{Primitive Cumulative Minimum}
\usage{
prim_cummin(operand, dim)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Arrayish value of any data type.}

\item{dim}{(\code{integer(1)})\cr
Dimension along which to accumulate (1-indexed).}
}
\value{
\code{list} of two \code{\link{arrayish}} values:\cr
The running minimum (same dtype as \code{operand}) and the running argmin
(dtype \code{i32}, 1-based). Both have the same shape as \code{operand}.
}
\description{
Running minimum of array elements along a single dimension along with
the index of the first occurrence of the running minimum.
At output position \code{j}, the values output is \verb{min(input[1..j])} and the
indices output is the smallest \code{i} in \verb{1..j} with
\code{input[i] == values[j]} (first-occurrence tiebreak, matching torch).
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to a variadic \code{\link[stablehlo:hlo_reduce_window]{stablehlo::hlo_reduce_window()}} over \verb{(values, iota)}
with a \code{(value < value | (value == value & idx < idx))} selector.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(matrix(c(3, 1, 4, 1, 5, 9), nrow = 2))
prim_cummin(x, dim = 1L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_cummin]{nv_cummin()}}
}
