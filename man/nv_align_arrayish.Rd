% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-utilities.R
\name{nv_align_arrayish}
\alias{nv_align_arrayish}
\title{Prepare Inputs of an API Function}
\usage{
nv_align_arrayish(...)
}
\arguments{
\item{...}{(\code{\link{arrayish}})\cr
Inputs to prepare.}
}
\value{
(\code{list})\cr
The inputs in the same order, each converted to an \code{\link{AnvilArray}} (or
left as-is for tracing boxes / abstract literals).
}
\description{
Convenience wrapper for the beginning of an \verb{nv_*} API function. It:
\enumerate{
\item Infers a common device from any concrete \code{\link{AnvilArray}} input and
converts \code{\link{arrayish}} R inputs (length-1 vectors, R arrays) onto it.
If none of the inputs is concrete, the R inputs are left as-is and
placed on the default device when they eventually hit \code{\link[=nv_array]{nv_array()}}.
\item Ensures that all concrete \code{\link{AnvilArray}} inputs share the same device
(and therefore the same backend). Cross-device inputs cause an error.
}

During tracing (inside \code{\link[=jit]{jit()}}) the conversion in step 1 is skipped
because inputs are abstract.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
nv_align_arrayish(nv_array(1:3), 1L)
\dontshow{\}) # examplesIf}
}
