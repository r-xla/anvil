% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flatten.R
\name{filter_list_node}
\alias{filter_list_node}
\title{Filter List Node}
\usage{
filter_list_node(tree, names)
}
\arguments{
\item{tree}{(\code{ListNode})\cr
A named list node as returned by \code{\link[=build_tree]{build_tree()}}.}

\item{names}{(character)\cr
Names of children to keep.}
}
\value{
A \code{ListNode} containing only the selected children with reindexed
leaves.
}
\description{
Subsets a \code{ListNode} to keep only the children whose names match \code{names},
then reindexes the leaf nodes so they map to contiguous positions in a flat
list. If all names are kept the original tree is returned unchanged.
}
\examples{
x <- list(a = 1, b = 2, c = 3)
tree <- build_tree(x)
sub <- filter_list_node(tree, c("a", "c"))
tree_size(sub)

unflatten(sub, x[c("a", "c")])
}
\seealso{
\code{\link[=build_tree]{build_tree()}}, \code{\link[=reindex_tree]{reindex_tree()}}, \code{\link[=unflatten]{unflatten()}}
}
