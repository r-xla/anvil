% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-xla.R
\name{AnvilBackendXla}
\alias{AnvilBackendXla}
\title{XLA backend}
\usage{
AnvilBackendXla()
}
\value{
An \code{\link{AnvilBackend}} object with subclass \code{"AnvilBackendXla"}.
}
\description{
Constructs the XLA backend, which stores array data in PJRT buffers (via
\code{\link[pjrt:pjrt_buffer]{pjrt::pjrt_buffer()}}) and compiles jitted functions to XLA executables
via \code{\link[=stablehlo]{stablehlo()}} and \code{\link[pjrt:pjrt_compile]{pjrt::pjrt_compile()}}. This is the default
backend.
}
\section{Data representation}{

An \code{\link{AnvilArray}} with \code{backend = "xla"} wraps a \code{\link[pjrt:pjrt_buffer]{pjrt::pjrt_buffer()}}
stored in the \verb{$data} field. The buffer owns the memory holding the tensor
values and may live on any device supported by PJRT (CPU, CUDA, Metal,
...). Calling \code{\link[=as_array]{as_array()}} transfers the buffer contents back to an R
array; calling \code{\link[=nv_array]{nv_array()}} on an R object uploads it to the requested
device.

Each \code{AnvilArray} therefore has an associated device, queryable via
\code{\link[=device]{device()}}. A device is a \code{\link[pjrt:as_pjrt_device]{pjrt::as_pjrt_device()}} object (e.g. the
platform \code{"cpu"} or \code{"cuda"}, optionally with an index such as \code{"cuda:1"}).
When \code{device} is \code{NULL} in \code{\link[=nv_array]{nv_array()}} or the \code{\link[=jit]{jit()}} wrapper, the
device defaults to the \code{PJRT_PLATFORM} environment variable (falling back
to \code{"cpu"}), or is inferred from the existing inputs of a jitted call.
Operations require all inputs to live on the same device.
}

\section{XLA JIT arguments}{

\itemize{
\item \code{donate} (\code{character()}, default \code{character()}): names of arguments whose
underlying buffers may be donated to (i.e., reused/consumed by) the
compiled XLA executable. Donated buffers must not be used again by the
caller after the call; this can reduce memory usage and copies for large
inputs. Must not overlap with \code{static}.
\item \code{device} (\code{NULL} | \code{character(1)} | \code{\link[pjrt:as_pjrt_device]{pjrt::PJRTDevice}},
default \code{NULL}): target device (e.g. \code{"cpu"}, \code{"cuda"}) on which the
function is compiled and executed. When \code{NULL}, the device is inferred
from the inputs; if inputs live on different devices an error is raised.
}
}

\seealso{
\code{\link[=AnvilBackend]{AnvilBackend()}}, \code{\link[=AnvilBackendQuickr]{AnvilBackendQuickr()}}, \code{\link[=local_backend]{local_backend()}}, \code{\link[=jit]{jit()}}.
}
