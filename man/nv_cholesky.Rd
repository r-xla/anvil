% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_cholesky}
\alias{nv_cholesky}
\title{Cholesky Decomposition}
\usage{
nv_cholesky(a, lower = TRUE)
}
\arguments{
\item{a}{(\code{\link{tensorish}})\cr
Symmetric positive-definite matrix with at least 2 dimensions.
The last two dimensions form the square matrix; any leading dimensions
are batch dimensions.}

\item{lower}{(\code{logical(1)})\cr
If \code{TRUE} (default), compute the lower triangular factor \code{L} such that
\code{a = L \%*\% t(L)}. If \code{FALSE}, compute the upper triangular factor \code{U}
such that \code{a = t(U) \%*\% U}.}
}
\value{
\code{\link{tensorish}}\cr
Triangular matrix with the same shape and data type as the input.
}
\description{
Computes the Cholesky decomposition of a symmetric positive-definite matrix.
Supports batched inputs: dimensions before the last two are batch dimensions.
}
\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  a <- nv_tensor(matrix(c(4, 2, 2, 3), nrow = 2), dtype = "f32")
  nv_cholesky(a)
})
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_solve]{nv_solve()}}, \code{\link[=nvl_cholesky]{nvl_cholesky()}}
}
