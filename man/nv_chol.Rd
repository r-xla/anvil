% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_chol}
\alias{nv_chol}
\title{Cholesky Decomposition}
\usage{
nv_chol(operand, lower = FALSE)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Symmetric positive-definite matrix with at least 2 dimensions.
The last two dimensions form the square matrix; any leading dimensions
are batch dimensions.}

\item{lower}{(\code{logical(1)})\cr
If \code{FALSE} (default, matching base R's \code{\link[base:chol]{base::chol()}}), compute the
upper triangular factor \code{U} such that \code{operand = t(U) \%*\% U}. If
\code{TRUE}, compute the lower triangular factor \code{L} such that
\code{operand = L \%*\% t(L)}.}
}
\value{
\code{\link{arrayish}}\cr
Triangular matrix with the same shape and data type as the input.
}
\description{
Computes the Cholesky decomposition of a symmetric positive-definite matrix.
Supports batched inputs: dimensions before the last two are batch dimensions.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
a <- nv_array(matrix(c(4, 2, 2, 3), nrow = 2), dtype = "f32")
nv_chol(a)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_solve]{nv_solve()}}, \code{\link[=prim_chol]{prim_chol()}}
}
