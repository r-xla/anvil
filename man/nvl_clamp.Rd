% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{nvl_clamp}
\alias{nvl_clamp}
\title{Primitive Clamp}
\usage{
nvl_clamp(min_val, operand, max_val)
}
\arguments{
\item{min_val}{(\code{\link{tensorish}})\cr
Minimum value (scalar or same shape as operand).}

\item{operand}{(\code{\link{tensorish}})\cr
Operand.}

\item{max_val}{(\code{\link{tensorish}})\cr
Maximum value (scalar or same shape as operand).}
}
\value{
\code{\link{tensorish}}
}
\description{
Element-wise clamp: max(min_val, min(operand, max_val)).
}
\section{Shapes}{

\code{min_val} and \code{max_val} must each be either scalar or the same shape as \code{operand}. The output has the same shape as \code{operand}.
}

\section{StableHLO}{

Calls \code{\link[stablehlo:hlo_clamp]{stablehlo::hlo_clamp()}}.
}

\examples{
\dontrun{
jit_eval({
  x <- nv_tensor(c(-1, 0.5, 2))
  nvl_clamp(nv_scalar(0), x, nv_scalar(1))
})
}
}
