% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R, R/rules-reverse.R
\name{nvl_lu}
\alias{nvl_lu}
\title{Primitive LU Decomposition}
\usage{
nvl_lu(operand)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Matrix of data type floating-point with exactly 2 dimensions.}
}
\value{
\code{list} of two \code{\link{arrayish}} values:\cr
\code{LU} has shape \verb{(m, n)} with the same dtype as the input;
\code{pivots} has shape \verb{(k,)} and dtype \code{i32} (1-based row swaps).
}
\description{
Computes the partial-pivoted LU decomposition of a matrix.
Factors a matrix \code{operand} of shape \verb{(m, n)} into a packed \code{LU} of the
same shape (lower-unit-triangular \code{L} strictly below the diagonal, upper-
triangular \code{U} on and above) plus a vector of \code{pivots} of length
\code{k = min(m, n)} such that \code{P \%*\% operand = L \%*\% U}, where \code{P} is the
permutation given by \code{pivots}.

Both \code{L} (with implicit unit diagonal) and \code{U} are stored in the single
\code{LU} output so the layout matches LAPACK's \code{getrf}. Use \code{\link[=nv_lu]{nv_lu()}} for an
R-friendly wrapper that splits them out.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_custom_call]{stablehlo::hlo_custom_call()}} with target \code{"anvil_lu"}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  x <- nv_array(matrix(c(4, 3, 6, 3), nrow = 2), dtype = "f64")
  nvl_lu(x)
})
\dontshow{\}) # examplesIf}
}
\references{
Walter, Sebastian (2012).
\emph{Structured higher-order algorithmic differentiation in the forward and reverse mode with application in optimum experimental design}.
Ph.D. thesis, Mathematisch-Naturwissenschaftliche Fakult"at II.
}
\seealso{
\code{\link[=nv_lu]{nv_lu()}}
}
