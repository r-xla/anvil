% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R, R/rules-reverse.R
\name{nvl_qr}
\alias{nvl_qr}
\title{Primitive QR Decomposition}
\usage{
nvl_qr(operand)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Matrix of data type floating-point with exactly 2 dimensions.}
}
\value{
\code{list} of two \code{\link{arrayish}} values:\cr
The first element is \code{Q} with shape \verb{(m, k)} and the second element is
\code{R} with shape \verb{(k, n)}, where \code{k = min(m, n)}. Both have the same
data type as the input.
}
\description{
Computes the reduced QR decomposition of a matrix.
Factors a matrix \code{operand} of shape \verb{(m, n)} into an orthogonal matrix \code{Q}
of shape \verb{(m, k)} and an upper triangular matrix \code{R} of shape \verb{(k, n)},
where \code{k = min(m, n)}, such that \code{operand = Q \%*\% R}.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_custom_call]{stablehlo::hlo_custom_call()}} with target \code{"anvil_qr"}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  x <- nv_array(matrix(1:6, nrow = 3, ncol = 2), dtype = "f32")
  nvl_qr(x)
})
\dontshow{\}) # examplesIf}
}
\references{
Walter, Sebastian (2012).
\emph{Structured higher-order algorithmic differentiation in the forward and reverse mode with application in optimum experimental design}.
Ph.D. thesis, Mathematisch-Naturwissenschaftliche Fakult"at II.
}
\seealso{
\code{\link[=nv_qr]{nv_qr()}}
}
