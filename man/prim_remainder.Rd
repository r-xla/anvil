% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_remainder}
\alias{prim_remainder}
\title{Primitive Remainder}
\usage{
prim_remainder(lhs, rhs)
}
\arguments{
\item{lhs, rhs}{(\code{\link{arrayish}})\cr
Arrayish values of data type integer, unsigned integer, or floating-point.
Must have the same shape.}
}
\value{
\code{\link{arrayish}}\cr
Has the same shape and data type as the inputs.
It is ambiguous if both inputs are ambiguous.
}
\description{
Element-wise IEEE-754 remainder of division: the sign of the result equals
the sign of the dividend, matching C \code{fmod}, JAX \code{lax.rem}, and StableHLO
\code{remainder}. This is \strong{not} the same as base R's \code{\%\%} (which floors and
takes the sign of the divisor); for example,
\code{prim_remainder(nv_array(1), nv_array(-3))} is \code{1}, while \code{1 \%\% -3} in
base R is \code{-2}.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_remainder]{stablehlo::hlo_remainder()}}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(7, 10, 15))
y <- nv_array(c(3, 4, 6))
prim_remainder(x, y)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_remainder]{nv_remainder()}}
}
