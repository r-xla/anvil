% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_inv}
\alias{nv_inv}
\title{Matrix Inverse}
\usage{
nv_inv(a)
}
\arguments{
\item{a}{(\code{\link{arrayish}})\cr
Square non-singular matrix.}
}
\value{
\code{\link{arrayish}}\cr
The inverse, same shape and dtype as \code{a}.
}
\description{
Computes \code{a^-1}, the inverse of a square non-singular matrix, by
solving \verb{a x = I}. Numerically equivalent to \code{solve(a)} in base R.

For most use cases prefer \code{\link[=nv_solve]{nv_solve()}} directly: forming the explicit
inverse is both slower and less numerically stable than solving against
a right-hand side.
}
\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  a <- nv_array(matrix(c(4, 3, 6, 3), nrow = 2), dtype = "f64")
  nv_inv(a)
})
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_solve]{nv_solve()}}
}
