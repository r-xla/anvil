% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flatten.R
\name{tree_path}
\alias{tree_path}
\title{Tree Path}
\usage{
tree_path(node, i, prefix = "")
}
\arguments{
\item{node}{(\code{Node})\cr
A tree node as returned by \code{\link[=build_tree]{build_tree()}}.}

\item{i}{(\code{integer(1)})\cr
The flat index of the leaf (as stored in \code{LeafNode$i}).}

\item{prefix}{(\code{character(1)})\cr
Path prefix. Used internally during recursion; callers should leave as \code{""}.}
}
\value{
A scalar \code{character} string.
}
\description{
Returns the human-readable path for a single leaf node identified by its
flat index. Only descends into the branch containing the target leaf,
making it efficient for error reporting.
}
\seealso{
\code{\link[=tree_paths]{tree_paths()}}, \code{\link[=build_tree]{build_tree()}}
}
