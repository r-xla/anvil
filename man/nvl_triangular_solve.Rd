% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R, R/rules-backward.R
\name{nvl_triangular_solve}
\alias{nvl_triangular_solve}
\title{Primitive Triangular Solve}
\usage{
nvl_triangular_solve(a, b, left_side, lower, unit_diagonal, transpose_a)
}
\arguments{
\item{a}{(\code{\link{arrayish}})\cr
Triangular coefficient matrix of data type floating-point with at least 2
dimensions. The last two dimensions must be equal (square matrix); any
leading dimensions are batch dimensions.}

\item{b}{(\code{\link{arrayish}})\cr
Right-hand side array. Must have the same data type, rank, and batch
dimensions as \code{a}.}

\item{left_side}{(\code{logical(1)})\cr
If \code{TRUE}, solve \code{op(a) \%*\% x = b}. If \code{FALSE}, solve \code{x \%*\% op(a) = b}.}

\item{lower}{(\code{logical(1)})\cr
If \code{TRUE}, \code{a} is lower triangular. If \code{FALSE}, \code{a} is upper triangular.}

\item{unit_diagonal}{(\code{logical(1)})\cr
If \code{TRUE}, assume diagonal elements of \code{a} are 1.}

\item{transpose_a}{(\code{character(1)})\cr
One of \code{"NO_TRANSPOSE"}, \code{"TRANSPOSE"}, or \code{"ADJOINT"}.}
}
\value{
\code{\link{arrayish}}\cr
Has the same shape and data type as \code{b}.
It is ambiguous if both \code{a} and \code{b} are ambiguous.
}
\description{
Solves a system of linear equations with a triangular coefficient matrix.
When \code{left_side} is \code{TRUE}, solves \code{op(a) \%*\% x = b} for \code{x}.
When \code{left_side} is \code{FALSE}, solves \code{x \%*\% op(a) = b} for \code{x}.
Dimensions before the last two are batch dimensions and must match
between \code{a} and \code{b} (no broadcasting).
Here \code{op} is \code{A} or \code{A^T} depending on \code{transpose_a}.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_triangular_solve]{stablehlo::hlo_triangular_solve()}}.
}

\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  # Solve L \%*\% x = b where L is lower triangular
  L <- nv_array(matrix(c(2, 0, 1, 3), nrow = 2), dtype = "f32")
  b <- nv_array(matrix(c(4, 3), nrow = 2), dtype = "f32")
  nvl_triangular_solve(L, b,
    left_side = TRUE, lower = TRUE,
    unit_diagonal = FALSE, transpose_a = "NO_TRANSPOSE"
  )
})
\dontshow{\}) # examplesIf}
}
\references{
Giles, Mike (2008).
\dQuote{An extended collection of matrix derivative results for forward and reverse mode automatic differentiation.}
Oxford University Computing Laboratory.
}
\seealso{
\code{\link[=nv_solve]{nv_solve()}}
}
