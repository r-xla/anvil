% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{nvl_argmin}
\alias{nvl_argmin}
\title{Primitive Argmin}
\usage{
nvl_argmin(operand, dim, drop = TRUE, index_dtype = "i32")
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Arrayish value of any data type.}

\item{dim}{(\code{integer(1)})\cr
Dimension along which to find the minimum.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop the reduced dimension from the output shape.
If \code{TRUE}, the reduced dimension is removed.
If \code{FALSE}, the reduced dimension is set to 1.}

\item{index_dtype}{(\code{character(1)})\cr
Data type for the returned indices. Default \code{"i32"}.}
}
\value{
\code{\link{arrayish}}\cr
Integer array of indices (1-based). Has \code{index_dtype} and the shape of
\code{operand} with \code{dim} removed (when \code{drop = TRUE}) or set to 1
(when \code{drop = FALSE}).
}
\description{
Returns the indices of the minimum values along a dimension.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_reduce]{stablehlo::hlo_reduce()}} with a comparator that tracks value-index
pairs, combined with \code{\link[stablehlo:hlo_iota]{stablehlo::hlo_iota()}} for index generation.
}

\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  x <- nv_array(c(3, 1, 2))
  nvl_argmin(x, dim = 1L)
})
\dontshow{\}) # examplesIf}
}
