% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_top_k}
\alias{nv_top_k}
\title{Top-K Elements}
\usage{
nv_top_k(x, k, dim = NULL)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
The array.}

\item{k}{(\code{integer(1)})\cr
Number of top elements to return. Must satisfy \verb{1 <= k <= shape(x)[dim]}.}

\item{dim}{(\code{integer(1)} | \code{NULL})\cr
Dimension along which to take the top \code{k}. If \code{NULL} (default),
uses the last dimension.}
}
\value{
\code{\link{arrayish}}\cr
Same shape as \code{x} except \code{dim} has size \code{k}. Values are sorted
in decreasing order along \code{dim}.
}
\description{
Returns the \code{k} largest values along a dimension, sorted in decreasing order.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(3, 1, 4, 1, 5, 9, 2, 6))
nv_top_k(x, k = 3L)

m <- nv_array(matrix(c(3, 1, 5, 2, 4, 0), nrow = 2, byrow = TRUE))
nv_top_k(m, k = 2L, dim = 2L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_top_k]{prim_top_k()}} for the underlying primitive, \code{\link[=nv_sort]{nv_sort()}}.
}
