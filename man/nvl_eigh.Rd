% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R, R/rules-reverse.R
\name{nvl_eigh}
\alias{nvl_eigh}
\title{Primitive Symmetric Eigendecomposition}
\usage{
nvl_eigh(operand)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Symmetric square matrix of floating-point data type.}
}
\value{
\code{list} of two \code{\link{arrayish}} values: \code{V} \verb{(n, n)} and \code{W} \verb{(n,)},
both with the same dtype as the input.
}
\description{
Computes the eigendecomposition of a symmetric (or Hermitian) matrix
\code{operand} of shape \verb{(n, n)}. Only the lower triangle is read.
Returns eigenvectors \code{V} (\verb{(n, n)}, columns are eigenvectors) and
eigenvalues \code{W} (\verb{(n,)}, ascending) such that
\code{operand = V \%*\% diag(W) \%*\% t(V)}.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_custom_call]{stablehlo::hlo_custom_call()}} with target \code{"anvil_eigh"}.
}

\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  x <- nv_array(matrix(c(2, 1, 1, 2), nrow = 2), dtype = "f64")
  nvl_eigh(x)
})
\dontshow{\}) # examplesIf}
}
\references{
Giles, Mike (2008).
\dQuote{An extended collection of matrix derivative results for forward and reverse mode automatic differentiation.}
Oxford University Computing Laboratory.

Magnus, R. J, Neudecker, Heinz (2007).
\emph{Matrix differential calculus with applications in statistics and econometrics}, 3rd edition.
John Wiley & Sons.
}
\seealso{
\code{\link[=nv_eigh]{nv_eigh()}}
}
