% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R, R/rules-reverse.R
\name{nvl_svd}
\alias{nvl_svd}
\title{Primitive Singular Value Decomposition}
\usage{
nvl_svd(operand)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Matrix of data type floating-point with exactly 2 dimensions.}
}
\value{
\code{list} of three \code{\link{arrayish}} values: \code{U} \verb{(m, k)}, \code{S} \verb{(k,)},
\code{Vt} \verb{(k, n)}. All have the same dtype as the input.
}
\description{
Computes the reduced ("economy") SVD of a matrix.
Factors a matrix \code{operand} of shape \verb{(m, n)} into \code{U} of shape \verb{(m, k)},
a vector of singular values \code{S} of length \code{k}, and \code{Vt} of shape
\verb{(k, n)}, where \code{k = min(m, n)}, such that
\code{operand = U \%*\% diag(S) \%*\% Vt}.

On the CUDA backend this primitive currently requires \code{m >= n} (cuSOLVER's
\code{gesvd} restriction). The host (LAPACK) backend supports any shape.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_custom_call]{stablehlo::hlo_custom_call()}} with target \code{"anvil_svd"}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  x <- nv_array(matrix(c(1, 0, 0, 1, 0, 1), nrow = 3), dtype = "f64")
  nvl_svd(x)
})
\dontshow{\}) # examplesIf}
}
\references{
Townsend, James (2016).
\dQuote{Differentiating the singular value decomposition.}
Imperial College London.

Walter, Sebastian (2012).
\emph{Structured higher-order algorithmic differentiation in the forward and reverse mode with application in optimum experimental design}.
Ph.D. thesis, Mathematisch-Naturwissenschaftliche Fakult"at II.
}
\seealso{
\code{\link[=nv_svd]{nv_svd()}}
}
