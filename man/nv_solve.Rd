% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_solve}
\alias{nv_solve}
\alias{solve.AnvilBox}
\title{Solve Linear System}
\usage{
nv_solve(a, b)

\method{solve}{AnvilBox}(a, b, ...)
}
\arguments{
\item{a}{(\code{\link{arrayish}})\cr
Square non-singular matrix.}

\item{b}{(\code{\link{arrayish}})\cr
Right-hand side, vector of length \code{n} or matrix with \code{n} rows. Must
have the same data type as \code{a}.}

\item{...}{Ignored; present for S3 method consistency with \code{\link[base:solve]{base::solve()}}.}
}
\value{
\code{\link{arrayish}}\cr
The solution \code{x} such that \code{a \%*\% x = b}.
}
\description{
Solves the linear system \code{a \%*\% x = b} for \code{x}. Uses LU decomposition
with partial pivoting internally, so \code{a} need only be square and
non-singular (it does not have to be symmetric or positive-definite).
}
\section{Shapes}{

\itemize{
\item \code{a}: \verb{(n, n)}
\item \code{b}: \verb{(n,)} or \verb{(n, k)}
\item output: same shape as \code{b}
}
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  a <- nv_array(matrix(c(4, 2, 2, 3), nrow = 2), dtype = "f32")
  b <- nv_array(matrix(c(1, 2), nrow = 2), dtype = "f32")
  nv_solve(a, b)
})
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nvl_lu]{nvl_lu()}}, \code{\link[=nvl_triangular_solve]{nvl_triangular_solve()}}
}
