% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_eigh}
\alias{nv_eigh}
\title{Symmetric Eigendecomposition}
\usage{
nv_eigh(a)
}
\arguments{
\item{a}{(\code{\link{arrayish}})\cr
Symmetric square matrix of floating-point data type.}
}
\value{
Named \code{list} with elements \code{V} (shape \verb{(n, n)}) and \code{W}
(length \code{n}).
}
\description{
Computes the eigendecomposition of a symmetric (or Hermitian) matrix \code{a}
of shape \verb{(n, n)}. Only the lower triangle of \code{a} is read.
Returns eigenvectors as columns of \code{V} and eigenvalues \code{W} in ascending
order, such that \code{a = V \%*\% diag(W) \%*\% t(V)}.

Uses a LAPACK / cuSOLVER custom call via the XLA FFI.
}
\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  x <- nv_array(matrix(c(2, 1, 1, 2), nrow = 2), dtype = "f64")
  nv_eigh(x)
})
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nvl_eigh]{nvl_eigh()}}
}
