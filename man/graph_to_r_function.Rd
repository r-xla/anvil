% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-to-r.R
\name{graph_to_r_function}
\alias{graph_to_r_function}
\title{Convert a Graph to an R function}
\usage{
graph_to_r_function(
  graph,
  include_declare = TRUE,
  pack_output = FALSE,
  target = c("base", "quickr")
)
}
\arguments{
\item{graph}{(\code{\link{Graph}})\cr
Graph to convert.}

\item{include_declare}{(\code{logical(1)})\cr
Whether to include a \code{declare(type(...))} call at the top of the function
body (useful for {quickr}). In plain R it is treated as a no-op. Default is \code{TRUE}.}

\item{pack_output}{(\code{logical(1)})\cr
Whether to force the function to return a single flat \code{double()} vector
containing all output leaves (for {quickr} compatibility when the graph
returns a nested output tree). Default is \code{FALSE}.}

\item{target}{(\code{character(1)})\cr
Code generation target. Use \code{"base"} for plain R evaluation and \code{"quickr"}
to restrict the generated code to a subset that can be compiled by {quickr}.}
}
\value{
(\code{function})
}
\description{
Converts (a supported subset of) \code{anvil::Graph} objects into a plain R
function that evaluates the same computation using base R operators.
}
\details{
This is intended as a bridge to tools like {quickr}, which can consume R
functions.
}
