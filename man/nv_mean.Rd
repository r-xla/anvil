% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_mean}
\alias{nv_mean}
\alias{mean.AnvlBox}
\alias{mean.AnvlArray}
\title{Mean}
\usage{
nv_mean(operand, dims = NULL, drop = TRUE)

\method{mean}{AnvlBox}(x, trim = 0, na.rm = FALSE, ...)

\method{mean}{AnvlArray}(x, trim = 0, na.rm = FALSE, ...)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Operand.}

\item{dims}{(\code{integer()} | \code{NULL})\cr
Dimensions to reduce. If \code{NULL} (default), reduces over all
dimensions, returning a scalar.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop reduced dimensions.}
}
\value{
\code{\link{arrayish}}\cr
Has the same data type as the input.
When \code{drop = TRUE}, the reduced dimensions are removed.
When \code{drop = FALSE}, the reduced dimensions are set to 1.
}
\description{
Computes the arithmetic mean along the specified dimensions. You can also
use \code{mean()}.
}
\details{
Implemented as \code{nv_reduce_sum(operand, dims, drop) / n} where \code{n} is the
product of the reduced dimension sizes.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(matrix(1:6, nrow = 2))
nv_mean(x)            # all dims -> scalar
nv_mean(x, dims = 1L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_reduce_sum]{nv_reduce_sum()}}
}
