% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_svd}
\alias{nv_svd}
\title{Singular Value Decomposition}
\usage{
nv_svd(a)
}
\arguments{
\item{a}{(\code{\link{arrayish}})\cr
Matrix of data type floating-point with exactly 2 dimensions.}
}
\value{
Named \code{list} with elements \code{U}, \code{S}, \code{Vt} of the shapes above.
}
\description{
Computes the reduced ("economy") SVD of a matrix.
Factors a matrix \code{a} of shape \verb{(m, n)} into \code{U} of shape \verb{(m, k)}, a
vector of singular values \code{S} of length \code{k}, and \code{Vt} of shape \verb{(k, n)},
where \code{k = min(m, n)}, such that \code{a = U \%*\% diag(S) \%*\% Vt}.

Uses a LAPACK / cuSOLVER custom call via the XLA FFI. The CUDA backend
currently requires \code{m >= n} (cuSOLVER \code{gesvd} restriction); the host
backend supports any shape.
}
\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  x <- nv_array(matrix(c(1, 0, 0, 1, 0, 1), nrow = 3), dtype = "f64")
  nv_svd(x)
})
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nvl_svd]{nvl_svd()}}
}
