% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flatten.R
\name{tree_diff}
\alias{tree_diff}
\title{Difference Between Trees}
\usage{
tree_diff(a, b, prefix = "")
}
\arguments{
\item{a, b}{(\code{Node})\cr
Trees to compare, as returned by \code{\link[=build_tree]{build_tree()}}.}

\item{prefix}{(\code{character(1)})\cr
Path prefix accumulated during recursion; callers should leave as \code{""}.}
}
\value{
\code{NULL} if \code{a} and \code{b} are structurally identical, otherwise a list
with elements \code{prefix} (the path to the divergence), \code{a}, and \code{b} (the
diverging subtrees).
}
\description{
Walks two trees in parallel and returns the first path/subtree pair where
they diverge, or \code{NULL} if they are structurally identical. The returned
\code{prefix} follows \code{\link[=tree_path]{tree_path()}} syntax.
}
\seealso{
\code{\link[=build_tree]{build_tree()}}, \code{\link[=tree_path]{tree_path()}}
}
\keyword{internal}
