% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_eigh}
\alias{prim_eigh}
\title{Primitive Symmetric Eigendecomposition}
\usage{
prim_eigh(operand)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Symmetric square matrix of floating-point data type.}
}
\value{
Named \code{list} with elements \code{values} (length \code{n}) and \code{vectors}
(shape \verb{(n, n)}). Both have the same dtype as the input.
}
\description{
Computes the eigendecomposition of a symmetric matrix \code{operand} of
shape \verb{(n, n)}:
\deqn{A = V \Lambda V^\top,}
where the columns of \eqn{V} are the (orthonormal) eigenvectors and
\eqn{\Lambda = \mathrm{diag}(\lambda_1, \ldots, \lambda_n)} is the
diagonal matrix of (real) eigenvalues in ascending order. Only the
lower triangle of \code{operand} is read. Returned as \code{list(values, vectors)} with names and order matching \code{\link[base:eigen]{base::eigen()}} — \code{values}
holds \eqn{\lambda_i} and \code{vectors} holds \eqn{V}.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_custom_call]{stablehlo::hlo_custom_call()}} with target \code{"eigh"}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(2, 1, 1, 2), shape = c(2, 2), dtype = "f64")
prim_eigh(x)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_eigh]{nv_eigh()}}
}
