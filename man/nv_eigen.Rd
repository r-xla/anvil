% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_eigen}
\alias{nv_eigen}
\title{Eigendecomposition (base-R-style wrapper)}
\usage{
nv_eigen(a, symmetric = TRUE, only.values = FALSE)
}
\arguments{
\item{a}{(\code{\link{arrayish}})\cr
Symmetric square matrix.}

\item{symmetric}{(\code{logical(1)})\cr
Must be \code{TRUE} (the default). Passing \code{FALSE} raises an error.}

\item{only.values}{(\code{logical(1)})\cr
Must be \code{FALSE} (no fast values-only path yet). Passing \code{TRUE}
raises an error.}
}
\value{
Named \code{list} with elements \code{values} (length \code{n}, descending)
and \code{vectors} (\verb{(n, n)}, columns are eigenvectors). Both have the
same dtype as \code{a}.
}
\description{
Convenience wrapper around \code{\link[=nv_eigh]{nv_eigh()}} that returns its result in the
shape of base R's \code{\link[base:eigen]{base::eigen()}}: a \code{list(values, vectors)} with
eigenvalues in \strong{descending} order and eigenvector columns reordered
to match.

Unlike \code{base::eigen()}, this only supports the symmetric / Hermitian
case (no \code{symmetric = FALSE} path). General non-symmetric
eigendecomposition would need complex-dtype outputs throughout the
anvl stack and is not yet supported. If \code{symmetric = FALSE} is passed
explicitly, an error is raised.
}
\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  a <- nv_array(matrix(c(2, 1, 1, 2), nrow = 2), dtype = "f64")
  nv_eigen(a)
})
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_eigh]{nv_eigh()}}, \code{\link[base:eigen]{base::eigen()}}
}
