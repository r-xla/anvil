% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{nvl_reduce}
\alias{nvl_reduce}
\title{Primitive Reduce}
\usage{
nvl_reduce(operand, init_value, dims, body, drop = TRUE)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Arrayish value of any data type.}

\item{init_value}{(\code{\link{arrayish}})\cr
Scalar initial value. Must have the same dtype as \code{operand}.}

\item{dims}{(\code{integer()})\cr
Dimensions to reduce over.}

\item{body}{(\code{function})\cr
Binary reduction function \code{f(accumulator, new_element)}.
Must return a scalar with the same dtype as \code{init_value}.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop the reduced dimensions from the output shape.
If \code{TRUE}, the reduced dimensions are removed.
If \code{FALSE}, the reduced dimensions are set to 1.}
}
\value{
\code{\link{arrayish}}\cr
Has the same data type as the input.
When \code{drop = TRUE}, the shape is that of \code{operand} with \code{dims} removed.
When \code{drop = FALSE}, the shape is that of \code{operand} with \code{dims} set to 1.
It is ambiguous if the input is ambiguous.
}
\description{
General-purpose reduction that applies a user-supplied binary function
along the specified dimensions. This is the most flexible reduce
primitive; the specialized variants (\code{\link[=nvl_reduce_sum]{nvl_reduce_sum()}}, \code{\link[=nvl_reduce_max]{nvl_reduce_max()}},
etc.) should be preferred when applicable.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_reduce]{stablehlo::hlo_reduce()}}.
}

\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
# Sum reduction written manually:
jit_eval({
  x <- nv_array(matrix(1:6, nrow = 2), dtype = "f32")
  nvl_reduce(x, nv_scalar(0, dtype = "f32"), dims = 1L,
    body = function(acc, new) acc + new)
})
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nvl_reduce_sum]{nvl_reduce_sum()}}, \code{\link[=nvl_reduce_max]{nvl_reduce_max()}}
}
