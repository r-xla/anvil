% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_logdet}
\alias{nv_logdet}
\alias{determinant.AnvilBox}
\title{Log Absolute Determinant}
\usage{
nv_logdet(a)

\method{determinant}{AnvilBox}(x, logarithm = TRUE, ...)
}
\arguments{
\item{a}{(\code{\link{arrayish}})\cr
Square matrix of floating-point data type.}

\item{x}{Same as \code{a} -- present for S3 method consistency with \code{\link[base:det]{base::determinant()}}.}

\item{logarithm}{(\code{logical(1)}) If \code{TRUE} (default), return the natural log of \verb{|det(x)|}; if \code{FALSE}, return \verb{|det(x)|} directly.}

\item{...}{Ignored; present for S3 method consistency with \code{\link[base:det]{base::determinant()}}.}
}
\value{
Scalar \code{\link{arrayish}} with the same dtype as \code{a}.
}
\description{
Computes \code{log(abs(det(a)))} of a square matrix in a numerically stable
way, using the LU decomposition. Returns \code{-Inf} for singular matrices.
This is the analogue of \code{determinant(a, logarithm = TRUE)$modulus} in
base R.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  a <- nv_array(matrix(c(4, 3, 6, 3), nrow = 2), dtype = "f64")
  nv_logdet(a)
})
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_det]{nv_det()}}, \code{\link[=nv_solve]{nv_solve()}}, \code{\link[=nvl_lu]{nvl_lu()}}
}
