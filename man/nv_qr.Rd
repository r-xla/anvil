% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_qr}
\alias{nv_qr}
\title{QR Decomposition}
\usage{
nv_qr(a)
}
\arguments{
\item{a}{(\code{\link{arrayish}})\cr
Matrix of data type floating-point with exactly 2 dimensions.}
}
\value{
Named \code{list} with elements \code{Q} and \code{R}:\cr
\code{Q} has shape \verb{(m, k)} and \code{R} has shape \verb{(k, n)}, where \code{k = min(m, n)}.
Both have the same data type as the input.
}
\description{
Computes the reduced QR decomposition of a matrix.
Factors a matrix \code{a} of shape \verb{(m, n)} into an orthogonal matrix \code{Q}
of shape \verb{(m, k)} and an upper triangular matrix \code{R} of shape \verb{(k, n)},
where \code{k = min(m, n)}, such that \code{a = Q \%*\% R}.

Uses a LAPACK-based custom call via the XLA FFI.
}
\examples{
\dontshow{if (pjrt::plugin_is_downloaded()) withAutoprint(\{ # examplesIf}
jit_eval({
  x <- nv_array(matrix(c(1, 2, 3, 4, 5, 6), nrow = 3), dtype = "f32")
  result <- nv_qr(x)
  result$Q
  result$R
})
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nvl_qr]{nvl_qr()}}
}
