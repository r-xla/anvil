% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_scan}
\alias{prim_scan}
\title{Primitive Scan}
\usage{
prim_scan(init, xs, body)
}
\arguments{
\item{init}{(\code{\link{arrayish}})\cr
Initial carry value.}

\item{xs}{(\code{\link{arrayish}})\cr
Per-step inputs. Iterated along the leading dimension; must have
non-zero rank.}

\item{body}{(\verb{function(carry, x)})\cr
Function called once per step. Receives the current \code{carry} and the
current row \code{x} of \code{xs} (with the leading dimension removed) and must
return \code{list(carry = ..., y = ...)}. The returned \code{carry} must match
\code{init} in dtype and shape.}
}
\value{
Named list with two elements:
\itemize{
\item \code{carry}: final carry, same dtype and shape as \code{init}.
\item \code{y}: per-step outputs stacked along a new leading dimension of size
\code{nv_shape(xs)[[1]]}.
}
}
\description{
Applies \code{body(carry, x)} sequentially over the leading dimension of \code{xs},
threading the \code{carry} state through each iteration and stacking the
per-iteration \code{y} outputs along a new leading dimension. This is the
analog of \code{\link[=Reduce]{Reduce()}} with \code{accumulate = TRUE}, and of JAX's \code{lax.scan}.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
}
}

\section{StableHLO}{

Lowers to a \code{\link[stablehlo:hlo_while]{stablehlo::hlo_while()}} loop that uses
\code{\link[stablehlo:hlo_dynamic_slice]{stablehlo::hlo_dynamic_slice()}} to read each row of \code{xs} and
\code{\link[stablehlo:hlo_dynamic_update_slice]{stablehlo::hlo_dynamic_update_slice()}} to write each row of \code{y}. The
body is lowered to a sub-function and invoked via \code{\link[stablehlo:hlo_call]{stablehlo::hlo_call()}}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
prim_scan(
  init = nv_scalar(0L),
  xs = nv_array(1:5),
  body = function(carry, x) {
    s <- carry + x
    list(carry = s, y = s)
  }
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_while]{prim_while()}}
}
