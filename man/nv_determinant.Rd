% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_determinant}
\alias{nv_determinant}
\title{Determinant in modulus/sign form}
\usage{
nv_determinant(operand, logarithm = TRUE)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Square matrix of floating-point data type.}

\item{logarithm}{(\code{logical(1)})\cr
If \code{TRUE} (default, matching base R), \code{modulus} is
\code{log(abs(det(operand)))}. If \code{FALSE}, \code{modulus} is \code{abs(det(operand))}.}
}
\value{
Named \code{list} with elements \code{modulus} and \code{sign}, both scalar
\code{\link{arrayish}} with the same dtype as \code{operand}. The full determinant
is \code{sign * exp(modulus)} (with \code{logarithm = TRUE}) or
\code{sign * modulus} (with \code{logarithm = FALSE}).
}
\description{
Computes the determinant of a square matrix in the modulus / sign
decomposition matching base R's \code{\link[base:det]{base::determinant()}}. For the plain
scalar determinant, use \code{\link[=nv_det]{nv_det()}}.
}
\details{
For computing the determinant, we use:
\deqn{P A = L U}
\deqn{\det(L) = 1}
\deqn{\det(A) = \det(U) / \det(P) = \mathrm{sign}(P^{-1}) \, \prod_i U_{ii}
  = \mathrm{sign}(P) \, \prod_i U_{ii}}
\deqn{\log|\det(A)| = \sum_i \log|U_{ii}|}
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
a <- nv_array(matrix(c(4, 3, 6, 3), nrow = 2), dtype = "f64")
nv_determinant(a)
nv_determinant(a, logarithm = FALSE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_det]{nv_det()}}, \code{\link[=nv_solve]{nv_solve()}}, \code{\link[=prim_lu]{prim_lu()}}
}
