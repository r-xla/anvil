% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-subset.R
\name{nv_update_subset}
\alias{nv_update_subset}
\title{Update Subset}
\usage{
nv_update_subset(x, ..., value)
}
\arguments{
\item{x}{(\code{\link{tensorish}})\cr
Input tensor to update.}

\item{...}{Subset specifications. Can be:
\itemize{
\item Ranges (e.g., \code{2:5}) for contiguous slices. Must be static.
\item Single integers (e.g., \code{3}) for single elements
\item Integer vectors (e.g., \code{c(1, 3, 6)}) for scattered indices
\item Missing (selects all) for full dimension
}}

\item{value}{(\code{\link{tensorish}})\cr
Values to write. Shape must match the subset shape.}
}
\value{
\code{\link{tensorish}}
}
\description{
Updates elements of a tensor with new values using scatter.
Supports both contiguous slices (e.g., \code{2:5}) and scattered indices (e.g., \code{c(1, 3, 6)}).
}
\examples{
\dontrun{
# Update contiguous slice
x <- nv_tensor(1:10)
x <- nv_update_subset(x, 2:4, value = nv_tensor(c(20, 30, 40)))

# Update scattered indices
x <- nv_tensor(1:10)
x <- nv_update_subset(x, c(1, 3, 6), value = nv_tensor(c(10, 30, 60)))
}
}
