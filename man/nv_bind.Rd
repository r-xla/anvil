% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-generics.R
\name{nv_bind}
\alias{nv_bind}
\alias{nv_rbind}
\alias{nv_cbind}
\alias{rbind.AnvlBox}
\alias{rbind.AnvlArray}
\alias{cbind.AnvlBox}
\alias{cbind.AnvlArray}
\title{Combine arrays by rows or columns}
\usage{
nv_rbind(...)

nv_cbind(...)

\method{rbind}{AnvlBox}(..., deparse.level = 1)

\method{rbind}{AnvlArray}(..., deparse.level = 1)

\method{cbind}{AnvlBox}(..., deparse.level = 1)

\method{cbind}{AnvlArray}(..., deparse.level = 1)
}
\arguments{
\item{...}{(\code{\link{arrayish}})\cr
Arrays to combine.}

\item{deparse.level}{Ignored. Kept for compatibility with \code{\link[base:cbind]{base::rbind()}}
and \code{\link[base:cbind]{base::cbind()}}.}
}
\value{
\code{\link{arrayish}}\cr
A rank-2 array of the common data type.
}
\description{
Combine arrays along the row (\code{nv_rbind}) or column (\code{nv_cbind}) dimension.
Arguments are promoted to a common data type before being combined.

Single-element inputs (rank 0, or rank 1 of length 1) are broadcast to
match the non-stacked dimensions of the other inputs, with size 1 along
the stacked dimension; if all inputs are single-element, they become
\code{c(1, 1)}. Inputs of rank 1 with length > 1 are treated as a single row
(for \code{nv_rbind}) or column (for \code{nv_cbind}). Inputs of rank >= 2 are
concatenated along dimension 1 (\code{nv_rbind}) or dimension 2 (\code{nv_cbind});
their other dimensions must match.

\code{\link[base:cbind]{base::rbind()}} and \code{\link[base:cbind]{base::cbind()}} dispatch to these via S3 when at least
one argument is an \code{\link{AnvlBox}} or \code{\link{AnvlArray}}.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(1:3)
y <- nv_array(4:6)
nv_rbind(x, y)
nv_cbind(x, y)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_concatenate]{nv_concatenate()}} for concatenation along an arbitrary dimension.
}
